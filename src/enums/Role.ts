export enum Role {
    BLOCK = -1, // Bloqueado
    GUEST = 0,  // Visitante
    APPLICANT = 1,  // Candidato
    USER = 10,  // Beneficiario
    RESEARCHER = 100, // Pesquisador
    ADMIN = 1000, // Administrador
}
